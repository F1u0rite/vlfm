[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my-vlfm"
version = "0.1.0"
description = "Standalone orchestration package for semantic navigation"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["my_vlfm"]

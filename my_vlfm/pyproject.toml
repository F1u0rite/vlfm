[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my-vlfm"
version = "0.1.0"
description = "Top-layer orchestration package built on top of vlfm"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["my_vlfm", "my_vlfm.my_vlfm"]
